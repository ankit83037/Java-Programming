import java.util.*;

public class SnacksDetails{
    
    public static void main (String[] args) {
        
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter the no of pizzas bought:");
        int pizzas = Integer.parseInt(sc.nextLine());
        
        System.out.println("Enter the no of puffs bought:");
        int puffs = Integer.parseInt(sc.nextLine());
        
        System.out.println("Enter the no of cool drinks bought:");
        int drinks = Integer.parseInt(sc.nextLine());
        
        System.out.println("Bill Details");
        System.out.println("No of pizzas:" + pizzas);
        System.out.println("No of puffs:" + puffs);
        System.out.println("No of cooldrinks:" + drinks);
        int total = pizzas*100 + puffs*20 + drinks*10;
        System.out.println("Total price=" + total);
        System.out.println("ENJOY THE SHOW!!!");
    } 
}
